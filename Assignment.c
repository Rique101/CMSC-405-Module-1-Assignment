// pre-processor directives

// include needed header file
#include "./headers/Assignment.h"

// write code for functions declared in header file

void heartRate(int, int){
    float targetHeartRate = (((220 - age) - restingPulse) * 0.50) + restingPulse

    // declare and initialize a counter variable
    // for our loop
    int i = START;

    // set up while loop using macro expression
    // as its condition
    while(START(i)){
        printf("%d ", i);
        i++;
    }

    return targetHeartRate;
}